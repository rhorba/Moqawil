# Accepted residual risks — reviewed 2026-08-08, re-check whenever this file is touched
# or at least once a sprint. Do not add an entry here without a written justification.

# image-size (transitive: docs/@docusaurus/mdx-loader) — DoS via crafted image buffers.
# No patched version exists upstream as of 2026-08-08 (Trivy status: "affected", no fix).
# Not attacker-reachable: image-size only runs at `docs/` (Docusaurus site) BUILD time,
# processing images embedded in maintainer-authored documentation content — never
# runtime input, never touched by the actual Moqawil web app or any end user.
CVE-2025-71329
CVE-2025-71330
