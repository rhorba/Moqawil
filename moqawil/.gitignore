# Dependencies
node_modules/
.pnp
.pnp.js

# Secrets — NEVER commit
.env
.env.local
.env.*.local

# Next.js build output
.next/
out/
dist/
build/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/
playwright-report/
test-results/

# Per-test debug videos (ephemeral). Versioned completion recordings
# (.recordings/v[version]-[date].webm, Framework Rule 7) are NOT ignored — commit those.
.recordings/e2e-debug/

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Drizzle migrations output (keep migration files, ignore generated snapshots)
packages/db/drizzle/meta/

# Editor
.vscode/settings.json
.idea/
